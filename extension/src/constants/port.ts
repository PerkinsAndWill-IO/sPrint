export enum PortMessage {
  EXPORT = "export",
  LOGIN = "login",
}

export enum PortResponse {
  EXPORTED = "exported",
  LOGED_IN = "loged-in",
}

export enum PortName {
  SPRINT = "sprint",
}
